@model Wasla.Models.CoinHistory

@{
    ViewData["Title"] = "Delete Coin History";
}

<h1>Delete Coin History</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">User</dt>
        <dd class="col-sm-10">@Model.User?.FullName</dd>

        <dt class="col-sm-2">Time</dt>
        <dd class="col-sm-10">@Model.Time</dd>

        <dt class="col-sm-2">Process Type</dt>
        <dd class="col-sm-10">@Model.ProcessType?.ProcessName</dd>

        <dt class="col-sm-2">Amount</dt>
        <dd class="col-sm-10">@Model.Amount</dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="UserId" />
        <input type="hidden" asp-for="Time" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
